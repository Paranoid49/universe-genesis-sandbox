# 质量门禁

状态：步骤 1 已完成；完整发布门禁通过，第二轮有限复查确认不存在有效 P0、P1、P2 或建议项。步骤 2 验收基线已经冻结，后续门禁必须以 `step-2-freeze.md` 为准新增，不得复用旧静态形状充当完成证据。

本文件同时约束当前旧基线兼容路径和新产品步骤。旧形状只在 [旧产品形状测试退役登记](legacy-test-retirement.md) 指定的替换步骤完成前有效；永久不变量和新步骤契约不得被旧页面、固定数量或固定本体断言替代。

## 1. 检查命令

日常完整检查只有一个入口：

```text
npm run check
```

它依次执行：

```text
eslint
应用与测试 TypeScript 类型检查
模拟核心单元测试与覆盖率
UI 组件测试与覆盖率
额外 UI 测试
生产构建
构建体积检查
```

发布级检查还必须执行真实浏览器端到端测试：

```text
npm run check:release
```

本地需要手动运行真实浏览器时，在一个可见终端执行：

```text
npm run build
npm run preview -- --port 4173
```

在另一个终端执行：

```text
npm run test:e2e
```

测试结束后必须在预览服务终端按 `Ctrl + C` 停止服务，并删除本次生成且不需要保留的 `coverage/`、`dist/`、`playwright-report/` 与 `test-results/`。

## 2. 测试分层

| 层级 | 权威职责 | 当前文件 |
| --- | --- | --- |
| 永久不变量 | 确定性、显式输入、核心隔离、因果闭包、错误可见性、只读投影和无障碍 | 新步骤契约测试与架构测试 |
| 步骤 1 契约 | 可见结果覆盖、根可达、规则来源、随机追踪、双向查询和闭包负向校验 | `tests/step-1-causality.test.ts` |
| 步骤 1 组件 | 文字路径、筛选、键盘、空状态与 axe | `tests/ui/causal-explorer.test.tsx` |
| 步骤 1 可见投影 | 观察摘要、几何、强度、事件关联、按点击追加，以及 Seed 对比左右独立因果入口 | `tests/ui/causal-projection-integration.test.tsx` |
| 步骤 1 纵向闭环 | 从主导航进入真实宇宙因果查询并双向追溯 | `tests/ui/app-interaction.test.tsx`、`tests/e2e/app.spec.ts` |
| 旧基线兼容 | 步骤 1 期间继续保护静态生成、旧页面、分享和 A1 图书馆 | `tests/phase-*.test.ts` 及旧 UI/E2E 用例 |
| 退役登记 | 逐条说明旧断言何时、由什么新契约接管 | `docs/legacy-test-retirement.md` |

固定事件、纪元、领域、指标、对象、文明、奇迹、叠层、页面或 A1 条目数量只属于旧基线兼容，不得作为新运行时发布标准。

## 3. 步骤 1 强制门禁

步骤 1 只有同时满足下列条件才能进入严格评审：

| 门禁 | 必须证明的事实 |
| --- | --- |
| 可见结果覆盖 | 每一个旧界面可见派生结果都有唯一可查询的因果节点或明确的一对多历史节点，不存在事实孤岛 |
| 合法根 | 无原因节点只能是宇宙公理、独立初始状态或有序显式外部输入，根列表与节点标记一致；模板配置和创世初态必须分别存在 |
| 直接原因与规则 | 每个非根节点至少有一个直接原因和一个有效法则／公理引用 |
| 根可达性 | 每个派生节点都能递归到合法根，不能用循环引用掩盖缺失原因 |
| 合法闭环 | 只允许由指定高阶公理、精确成员、精确内部边、全部一致性约束和外部合法根路径共同授权的闭环；其余循环全部拒绝 |
| 双向一致 | 边端点存在，节点原因／后果索引与边集合完全一致，双向查询返回相同关系 |
| 精确随机证据 | 算法版本、流 ID、完整命名空间、一基抽样序号、类型化操作参数、候选顺序、权重、选中值和用途稳定；受控工厂重新执行完整生成并逐项核对调用转录 |
| 随机结果绑定 | 每个随机引用绑定结果节点、结果主题、节点类型、类型化领域定位器、领域值指纹、原始作用域和字段／集合／负事实语义；生产认证必须解析真实领域结果，同步修改结构副本不得重新获得认证 |
| 输入根真实性 | 因果图保存规范化、有序且类型明确的显式输入清单；输入根与清单一一对应，生成身份同时绑定随机追踪，额外、缺失、重排和值错配全部拒绝 |
| 确定性 | 同一 Seed、模板、规则版本和有序干预输入产生完全一致的旧字段和因果图 |
| 同果异因 | 显示值或状态相同不得合并不同原因节点或不同历史 |
| 展示降级 | 名称、解释和观察文案只能是派生投影，不能成为事实根或反向驱动模拟 |
| 双生成入口 | `generateUniverse` 只承担非枚举惰性兼容边界；当前产品必须使用 `generateCausalUniverse`，在返回前强制物化、校验并冻结因果图 |
| 运行时不可变 | `causalGraph` 非枚举且不可配置，图、节点、边、随机决策、闭环授权和嵌套集合全部运行时冻结 |
| 生产拒绝 | 认证写入能力只存在于私有闭包；基础图必须通过完整领域重放、随机转录、独立结果绑定和闭包断言；投影只能从已认证父图派生为非生产视图 |
| 认证结果不可污染 | 生产图的公共成功校验结果与问题数组不可修改；非认证图每次返回独立问题结果，调用方修改不得影响后续校验或序列化 |
| 按需展示投影 | 普通观察与法则页面渲染不得批量追加因果图；只有点击具体原因入口时追加一个投影节点并聚焦查询 |
| 跨宇宙比较 | 每项 Seed 法则差异与总结先建立组合证据，绑定左右生成身份、左右图来源节点、两侧评分、差值和固定顺序裁决；左右入口分别展示本侧依据且不得合并随机追踪 |
| 用户闭环 | 玩家可以从一级入口选择真实结果，分别查看原因和后果，并沿文字路径继续查询 |
| 旧页面就地追因 | 实际渲染的结果标记必须拥有同主题生产入口或按需状态值投影；事件效果与观察关联不得借用宽实体入口；返回后保留筛选、分页、选中对象、零／非零滚动位置和唯一焦点令牌 |
| 无障碍 | 因果入口、结果列表、方向标签和路径操作支持键盘，组件与真实浏览器 axe 无严重违规 |
| 兼容性 | 旧创世、主要浏览、UGS070/I1 分享和 A1 图书馆仍可运行，且因果图不改变旧领域字段 |
| 旧测试换轨 | 每条旧形状断言已分类并登记退出步骤，步骤 1 不再把固定数量解释为永久要求 |

因果校验器每一个公开问题码都必须有直接负向测试，至少包括：

- 重复节点与重复边。
- 悬空边端点。
- 非法根、根有原因和根列表不一致。
- 无直接原因、无规则引用与未知规则引用。
- 双向邻接不一致。
- 无法到达合法根。
- 未授权循环、缺少成员或边的授权、缺少高阶公理、缺少一致性约束和没有外部根路径的伪授权循环。
- 无效随机流或抽样区间。
- 决策 ID、操作、样本序号、候选集标识或选中值与随机追踪不一致。
- 类型化范围、概率、候选顺序或权重与兼容表示不一致。
- 随机引用与结果绑定表不一致。
- 决策实际所属随机流与引用声明的流不一致。
- 显式输入清单缺失、额外、重排、类型或值错配，以及清单生成身份与随机追踪不一致。

产品定义允许由高阶公理和一致性约束明确许可的因果闭环，因此步骤 1 必须同时保留“有效授权通过”和“无授权／无效授权拒绝”两组测试，不再允许用评审说明代替可执行契约。

步骤 1 当前具有独立性能契约，非覆盖率门禁在两次预热后检查十二个多模板样本的 P50 与 P90 均小于 1500 毫秒，覆盖率环境代表性场景预算为 3000 毫秒；该门禁已进入 `check:release`。

## 4. 自动化阈值

| 门禁 | 当前要求 |
| --- | --- |
| ESLint | TypeScript 推荐规则、React Hooks 规则、禁止显式 `any` 和未使用变量 |
| 模拟覆盖率 | 语句、函数、行不低于 90%，分支不低于 82% |
| UI 全局覆盖率 | 语句、函数、行不低于 75%，分支不低于 70% |
| UI 关键文件 | 采用 `vitest.ui.config.ts` 中针对 App、页面、空间浏览、观察投影、存档和主 view-model 的更高独立阈值 |
| 架构边界 | TypeScript AST 检查真实依赖、生成器调用、浏览器状态访问和模拟核心导入循环 |
| 文件规模 | 所有关键编排文件、因果构建／查询文件、因果 UI 模型、因果组件和分拆样式都有显式行数上限 |
| 真实浏览器 | Chromium、Firefox、WebKit 和移动 Chromium 覆盖核心纵向闭环 |
| 构建 | TypeScript 与 Vite 生产构建成功 |
| 受控 UI 运行时 | 生产与 UI 测试使用 Preact React-compat，React 18.3.1 与对应类型仅作为源码和工具兼容契约；别名、锁文件、UI、E2E、构建和审计必须一致 |
| 体积 | JavaScript gzip 硬上限 115 KiB、保护线 110 KiB；CSS gzip 硬上限 10 KiB、保护线 8 KiB；本轮数字由完整发布门禁重新生成，预算与运行时决策见正式决策文档 |

不能通过降低覆盖率、文件规模、性能或体积阈值来绕过失败。构建体积脚本累加全部 JavaScript 产物，单纯延迟加载不能降低总体积；若新增能力突破保护线，应精简依赖、删除重复实现，或通过正式架构决策调整预算模型与检查方式。

历史门禁数字只保留在对应 `docs/reviews/*-remediation.md` 中，不再与当前发布事实并列维护。

当前定向测试组成：步骤 1 契约测试 21 项；生产工厂守卫 2 项；架构测试 9 项；合计 32 项。

当前发布快照：模拟测试 104 项；模拟覆盖率 92.26%／83.44%／94.52%／95.72%；UI 测试 44 项；UI 覆盖率 89.96%／81.96%／92.3%／92.76%；独立性能测试 1 项；E2E 54 项通过、2 项跳过；JavaScript gzip 87406 字节；CSS gzip 5828 字节。

Preact React-compat 是受控而非永久生产运行时，兼容边界、验证要求和回退条件见 [步骤 1 用户界面运行时体积决策](decisions/ui-runtime-size-step-1.md)。每个产品步骤开始前都必须复核继续使用、迁回 React 或采用其他运行时的证据。

## 5. 架构与边界门禁

`tests/architecture.test.ts` 必须持续验证：

- 模拟核心不依赖 React、UI、组件、App 或浏览器状态。
- 展示组件不直接调用生成器。
- UI、组件和 App 保持向上单向依赖。
- 模拟核心内部无导入循环。
- 本地存储只由当前存储适配器访问；步骤 2 替换存储后同步更新唯一适配器边界。
- 关键文件不超过职责规模。

步骤 1 新增的以下文件必须进入规模审计：

- `src/sim/causality.ts`
- `src/sim/causality-generation.ts`
- `src/sim/causal-comparison.ts`
- `src/sim/causality-builder.ts`
- `src/sim/causality-freeze.ts`
- `src/sim/causality-projection.ts`
- `src/sim/causality-query.ts`
- `src/sim/causality-random-bindings.ts`
- `src/sim/causality-random-binding-validation.ts`
- `src/sim/causality-domain-locators.ts`
- `src/sim/random-transcript.ts`
- `src/sim/causality-source.ts`
- `src/sim/contracts/causality.ts`
- `src/sim/contracts/causal-locators.ts`
- `src/sim/contracts/causal-comparison.ts`
- `src/sim/contracts/random.ts`
- `src/ui/causalView.ts`
- `src/ui/causalProjectionSources.ts`
- `src/ui/observationCausalProjection.ts`
- `src/ui/lawComparisonCausalProjection.ts`
- `src/ui/useLawComparisonModel.ts`
- `src/components/CausalExplorer.tsx`
- `src/components/common.tsx`
- `src/components/CausalRandomEvidence.tsx`
- `src/components/causalExplorerModel.ts`
- `src/styles-causality.css`
- `src/styles-causality-responsive.css`

## 6. 变更纪律

- 修改 `src/sim/**` 时必须判断是否改变确定性输出、因果图或公共契约。
- 生成规则、随机算法、因果契约或输出字段发生变化时，必须同步评估规则版本、规则短码、内容哈希、因果契约版本和兼容策略。
- 仅修改 UI、测试或文档不得改变模拟核心内容哈希；领域结果与因果证据契约必须分别评估对应版本。
- 删除或替换旧行为前，必须先提供新契约测试、迁移测试或明确拒绝测试。
- 归档文档只用于历史说明，不能作为当前完成证据。
- 测试成功不等于步骤完成；还必须核对产品闭环、迁移矩阵、退役登记、当前架构和非功能要求。

## 7. 严格架构评审

每个新产品步骤完成实现与完整检查后，必须启动一个此前未参与该步骤实现的全新独立 Agent，只依据该步骤冻结的验收基线进行评审。

评审必须：

- 按易用性、可扩展性、可维护性、边界清晰度、阶段功能完整度、文档明确度、测试有效性、数据正确性和运行可靠性分别评分。
- 将问题分为 P0、P1、P2 和建议项。
- 为每项问题提供涉及位置、触发或复现方式、实际影响、判断依据和整改建议。
- P0 和 P1 必须引用冻结验收要求或必须保持的核心不变量；无法给出现实触发路径、具体影响或验证证据的问题只能归类为建议项。
- 评审不得增加冻结基线中不存在的需求，也不得评价后续步骤。
- 把报告保存到 `docs/reviews/step-N-review-XX.md`。

有效 P0 和 P1 必须关闭，P2 必须修复、以证据驳回、降级或登记技术债并给出明确处置。每轮整改后使用另一个全新 Agent 有限复查尚未关闭问题、本轮修改及直接回归，不得复用上一轮评审者或重新开放式评审。建议项不阻塞步骤完成，但不得把实际 P2 改名规避处置。
