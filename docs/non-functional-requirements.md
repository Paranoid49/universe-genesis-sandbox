# 非功能需求

状态：步骤 1 至步骤 6 已完成；步骤 6 首次独立评审问题经过三轮有限复查全部关闭。

步骤 2 当前发布快照：模拟测试 126 项；模拟覆盖率 92.53%／83.8%／94.93%／96.19%；UI 测试 66 项；UI 覆盖率 89.72%／82.08%／91.37%／93.02%；独立性能测试 3 项；E2E 62 项通过、2 项跳过；JavaScript gzip 96633 字节；CSS gzip 6080 字节。
步骤 2 当前定向测试组成：步骤 2 核心契约 19 项；步骤 2 架构 3 项；步骤 2 UI 与存储 22 项；步骤 2 性能 2 项；步骤 2 四浏览器纵向闭环 8 项；合计 54 项。

步骤 3 第三轮整改发布快照：模拟测试 142 项；模拟覆盖率 92.6%／84.62%／95.13%／95.99%；UI 测试 76 项；UI 覆盖率 88.37%／80.88%／88.23%／93.04%；独立性能测试 4 项；E2E 75 项通过、5 项按能力跳过；JavaScript gzip 104875 字节；CSS gzip 6318 字节。
步骤 3 当前定向测试组成：观察与研究契约 12 项；架构 4 项；UI、研究存储与存档页面 10 项；观察性能 1 项；四浏览器观察、研究、窄屏、错误与响应纵向场景 20 项；合计 47 项。

步骤 4 第二轮整改发布快照：模拟测试 165 项；模拟覆盖率 92.47%／84.79%／95.12%／96.07%；UI 测试 98 项；UI 覆盖率 89.82%／80.09%／88.69%／93.70%；独立性能测试 5 项；E2E 87 项通过、5 项按能力跳过；JavaScript gzip 112627 字节；CSS gzip 6326 字节。
步骤 4 当前定向测试组成：分支核心与负向契约 19 项；架构 4 项；分支与存储 UI 18 项；分支性能 1 项；四浏览器分支、分享与研究隔离纵向场景 12 项；合计 54 项。

步骤 5 第三轮整改发布快照：模拟测试 193 项；模拟覆盖率 92.64%／85.11%／95.49%／96.36%；UI 测试 102 项；UI 覆盖率 90.40%／79.42%／89.47%／94.40%；独立性能测试 6 项；E2E 91 项通过、5 项按能力跳过；JavaScript gzip 123319 字节；CSS gzip 6492 字节。
步骤 6 最终发布快照：模拟测试 250 项；模拟覆盖率 92.87%／85.33%／95.61%／96.69%；UI 测试 109 项；UI 覆盖率 90.75%／81.17%／90.34%／94.87%；独立性能测试 7 项；E2E 99 项通过、5 项按能力跳过；JavaScript gzip 127972 字节；CSS gzip 6492 字节。

本文件是性能、兼容性、无障碍、可靠性和错误处理的当前事实来源。涉及固定模板、预制时间线、传统空间、奇迹、I1 分享和 A1 图书馆的要求只保护旧基线，退出时间以 [旧产品形状测试退役登记](legacy-test-retirement.md) 为准。

## 1. 性能与容量

- 旧基线普通测试环境中连续生成 50 个静态宇宙应在 2 秒内完成，覆盖率插桩环境允许 5 秒；该预算只保护隔离兼容路径，不证明运行时性能。
- 兼容入口 `generateUniverse` 采用首次访问时重放、构建并缓存因果图的策略；产品入口 `generateCausalUniverse` 必须在返回宇宙前强制物化、校验并冻结因果图。
- 因果查询必须保留完整底层图。界面可以分批渲染，但筛选、直接关系和双向遍历必须能够访问全部节点。
- 当前因果界面首批展示 80 个结果、最多渲染 24 条完整路径并预览 80 个可达节点；发生截断时必须明确告知，并提供继续筛选或查看关系的文字路径。
- 观察与 Seed 对比的界面投影只能在玩家点击具体原因入口时调用 `appendCausalProjections`；普通页面渲染不得为全部坐标、强度、事件关联或左右对比结果预先复制完整因果图。
- 生产构建后的 JavaScript gzip 总体积不得超过 130 KiB，日常保护线为 125 KiB；初始预算依据见 [步骤 1 JavaScript 体积预算决策](decisions/bundle-budget-step-1.md)，步骤 5 调整依据见 [步骤 5 JavaScript 体积预算决策](decisions/bundle-budget-step-5.md)。
- 生产构建后的 CSS gzip 总体积不得超过 10 KiB，日常保护线为 8 KiB。
- 生产与 UI 测试采用 Preact 原生渲染与 Hooks；旧 Node SSR 测试通过测试专用别名使用 React Hooks，当前完整构建为 JavaScript gzip 127972 字节、CSS gzip 6492 字节；依据见 [步骤 1 用户界面运行时体积决策](decisions/ui-runtime-size-step-1.md)。

步骤 1 发布快照：模拟测试 104 项；模拟覆盖率 92.26%／83.44%／94.52%／95.72%；UI 测试 44 项；UI 覆盖率 89.96%／81.96%／92.3%／92.76%；独立性能测试 1 项；E2E 54 项通过、2 项跳过；JavaScript gzip 87406 字节；CSS gzip 5828 字节。
- 任意观察和可视化能力必须保留可操作的文字路径，不得要求先加载重型可视化运行时。
- 步骤 2 单步转换 P50 小于 50 毫秒、P90 小于 100 毫秒；连续一百步 P50 小于 1500 毫秒、P90 小于 2500 毫秒；快照 P90 小于 500 毫秒；恢复与完整性校验 P90 小于 1000 毫秒。
- 步骤 2 在 1、2、4、8 倍速度下，暂停命令必须在当前时间步提交完成后 100 毫秒内取消下一步调度。
- 连续运行一次最多自动调度 100 个完整时间步，达到预算后自动暂停并提示；再次启动只开启新的调度批次，不跳过状态转换或因果记录。
- 步骤 3 单次观察投影 P50 小于 50 毫秒、P90 小于 100 毫秒；一百条观察历史重建 P50 小于 500 毫秒、P90 小于 1000 毫秒；研究记录保存 P90 小于 500 毫秒；恢复并完成完整性校验 P90 小于 1000 毫秒。
- 观察筛选、关注和笔记输入的页面反馈在普通桌面环境中 100 毫秒内可见；大量研究记录必须采用分页、索引或等价预算控制，不得删除证据、改变顺序或跳过完整性校验。
- 步骤 4 从不超过一百步的有效状态创建分支 P50 小于 500 毫秒、P90 小于 1000 毫秒；一百步完整重放 P50 小于 1500 毫秒、P90 小于 2500 毫秒；共同祖先分支比较 P50 小于 500 毫秒、P90 小于 1000 毫秒；分支保存 P90 小于 750 毫秒，恢复与完整性校验 P90 小于 1500 毫秒。
- 分支切换、比较筛选和实验参数输入的页面反馈在普通桌面环境中 100 毫秒内可见；浏览器存储直接支持同一宇宙二十条分支，达到上限时明确拒绝且不删除旧值。
- 步骤 5 宪法规范化与完整校验 P90 小于 100 毫秒，初始状态创建 P90 小于 500 毫秒，一百步演化 P90 小于 2500 毫秒，动态观察 P90 小于 100 毫秒，存档恢复与完整性校验 P90 小于 1500 毫秒。
- 步骤 6 启用不超过三十二个主体时，一百步完整演化 P50 小于 2000 毫秒、P90 小于 3500 毫秒；单次自主认知与行动批次 P90 小于 100 毫秒；包含自主状态和语义重放的存档恢复 P90 小于 2000 毫秒。
- 步骤 7 必须定义长期分支数量、因果历史规模、IndexedDB、导入导出和迁移预算。

步骤 1 的独立非覆盖率性能门禁执行两次预热后，测量十二个多模板样本的完整生成、首次强制物化和双向查询，P50 与 P90 均须小于 1500 毫秒。覆盖率环境继续用代表性场景验证同一路径小于 3000 毫秒；仅依赖 50 个未访问惰性因果图的旧生成性能测试不能证明因果查询性能。

步骤 2 的独立非覆盖率性能门禁同样执行两次预热和十二个多模板样本，同时测量单步、一百步、快照与恢复；性能测试必须验证恢复状态与原状态完全一致，优化不得改变状态序列、随机决定、因果节点或存档内容。

步骤 3 的独立非覆盖率性能门禁同样执行两次预热和至少十二个代表性样本，同时测量观察投影、百条历史重建、研究保存与恢复；性能优化不得改变信号身份、可见值、证据强度、来源引用、研究内容或条目顺序。

步骤 4 的独立非覆盖率性能门禁执行两次预热和十二个代表性样本，同时测量分支创建、百步重放、共同祖先比较、分支序列化与完整恢复；性能优化不得改变分支身份、状态哈希、历史哈希、输入顺序、随机决定或因果网络。

步骤 6 的独立非覆盖率性能门禁使用三十二主体宪法夹具、两次预热和十二个正式样本，同时测量主体形成行动批次、百步演化和完整存档恢复；性能优化不得删除认知历史、跳过最终完整性校验、改变行动顺序或放宽引用拒绝条件。

步骤 1 定向测试组成：步骤 1 契约测试 21 项；生产工厂守卫 2 项；架构测试 9 项；合计 32 项。

## 2. 兼容性

- 支持具备 ES2022、Web Crypto、URLSearchParams 和 Clipboard API 的现代桌面及移动浏览器。
- 页面最低适配宽度为 360 像素，当前响应式断点为 1180 像素与 720 像素。
- 因果查询在窄屏下不得产生页面级横向溢出，结果选择、方向切换和文字路径仍须可操作。
- 剪贴板 API 不可用时必须提供手动复制回退。
- 非法表单输入必须在页面内显示可访问错误，不得让同步生成异常逃逸到渲染层。
- 应用顶层必须提供未预期异常恢复界面。
- 模拟核心不得读取浏览器、网络、本地存储或系统时间状态。
- 当前生产与 UI 测试运行时为 Preact 原生渲染与 Hooks；React 18.3.1 与对应类型只提供旧 Node SSR、源码和开发工具兼容契约，生产构建不得同时打入 React 与 Preact 两套运行时。生产构建面向 ES2022，并使用 Terser 多轮压缩；所有用于强制物化惰性因果图的属性读取必须实际进入 `assertCausalGraph`，不得依赖可被压缩器删除的无结果读取。
- 默认运行产品壳不装配旧静态生成器；只有玩家进入“旧版隔离兼容视图”或打开旧分享查询时才按需生成步骤 1 静态宇宙。
- 旧 UGS070、I1 与 A1 不得自动升级为等价运行历史；旧分享查询直接进入隔离兼容视图。
- 当前运行、正式分支和研究记录分别使用 `ugs.runtime.v5`、`ugs.branches.v5` 和 `ugs.research.v3` IndexedDB 数据库，运行与分支存储适配器为版本 5，研究存储适配器为版本 3；不得写入旧 A1 `localStorage`，也不得静默读取旧步骤数据并声明历史等价。

步骤 1 的兼容边界如下：

- 规则版本为 `ugs-ruleset@0.7.0`，短码为 `UGS070`。
- 因果契约版本为 `ugs-causality@13`，随机结果绑定版本为 `ugs-random-bindings@5`，显式输入清单版本为 `ugs-causal-inputs@1`，跨宇宙比较证据版本为 `ugs-law-comparison-evidence@1`，随机算法版本为 `fnv1a32-mulberry32@1`。
- 当前模拟核心内容哈希基线为 `ba2f3b893d1e88274ee1c97888b0a181c26d58aac6ccb20d81be429b6aea6aea`；步骤 6 新运行时使用版本化自主协议、认知历史、行动记录、关系、叙述、神话档案和自主因果，旧领域 JSON 继续由隔离兼容路径读取，旧规则版本仍为 `ugs-ruleset@0.7.0`。
- 观察与解释投影属于冻结的非生产视图，不得通过事实图公共认证、序列化或跨宇宙事实比较。
- `UniverseSummary` 继续作为旧页面兼容读取模型，并新增不可配置、非枚举、惰性缓存且运行时深度冻结的只读 `causalGraph`；旧 JSON、A1 和分享载荷不得隐式序列化整张因果图。
- UGS070 分享、I1 干预载荷、A1 图书馆和旧页面暂时保持可用。
- A1 仍只保存分享码和少量用户元数据，不把因果图冒充可继续运行的状态存档。
- 不支持的规则、因果、分享或存档版本必须明确拒绝、受控迁移或静态只读打开，不得按当前规则重建后声称等价。

UI 运行时必须在以下任一条件出现时重新评估：新步骤需要 React 专有能力、关键依赖无法在 Preact React-compat 下正确运行、真实浏览器出现无法局部关闭的兼容缺陷、旧界面拆除后 React 能重新满足体积保护线，或者总体积预算模型发生正式变更。每个产品步骤开始前必须复核。

## 3. 无障碍

- 目标为 WCAG 2.1 AA 的核心交互要求。
- 创世、导航、因果查询、观察、实验、干预、分支和存档控件必须具有可访问名称。
- 因果结果列表必须支持键盘进入、方向键连续选择和可见焦点。
- “为什么发生”和“产生了什么后果”必须使用正确的标签与面板语义，并支持键盘切换。
- 所有因果关系、规则和抽样证据必须有纯文字路径，不能只依赖颜色、图标、连线或悬停。
- 空原因、空后果、缺失节点、循环和路径截断必须提供可读说明。
- 自动化组件与真实浏览器测试不得出现 axe 可检测的严重无障碍违规。

## 4. 确定性与数据完整性

- 完备原因必须产生完全一致的结果和因果图。
- 随机算法版本、Seed 指纹、流 ID、完整命名空间、样本数量和一基抽样序号必须稳定；每次实际决策还必须记录操作类型、类型化范围／概率／候选／权重参数、可选领域作用域、候选集标识、受控候选快照、原始样本和选中值。
- 因果图构建和查询不得消费新的随机样本。
- 每个因果随机引用只能绑定一条实际决策，并同时通过流、命名空间、样本序号、候选集标识和选中值校验，不能引用一整段命名空间作为近似原因。
- 随机引用中的 `decisionId` 必须实际存在于其声明的 `streamId` 中，不能拼接其他随机流中数值相同的决策。
- 每个引用必须具有独立结果绑定，保存结果节点、结果主题、节点类型、类型化领域定位器、领域值指纹、作用域和字段／集合／负事实语义；生产认证必须针对受控重放结果解析定位器并核对领域语义，不能使用待认证图自报的期望值。
- 随机调用转录必须通过不授予认证能力的逐字段比较器核对六类操作、范围边界、概率、候选顺序、候选内容、标签和权重。
- 认证写入能力不得通过命名导出、默认导出、导出类、静态或实例方法、导出赋值及其 helper、别名、属性、回调和解构绕行公开，普通结构夹具、克隆、同步修改副本和重新冻结对象均不能自行获得生产认证。
- 公共成功认证结果必须冻结且不可污染；普通失败结果每次独立创建，调用方修改不得改变后续校验和序列化结论。
- 相同显示值不得成为合并因果节点或历史的依据。
- 因果节点、边、规则引用、双向邻接、根列表和随机引用必须在进入产品查询路径前通过闭包校验。
- 模板配置和创世初态必须作为两个独立 `initial_state` 根进入闭包，不能与 Seed、模板选择或规则版本等外部输入混同。
- 所有 `input` 根必须与规范化生成清单一一对应；清单按 Seed、规则版本、创世模板和有序干预排列，并与随机追踪共享同一生成身份。额外、缺失、重排、类型错配、值错配或跨宇宙随机追踪必须拒绝。
- 因果闭环只有在精确声明成员节点、内部边、授权高阶公理、全部一致性约束且存在外部合法根路径时才允许；未经授权或授权不完整的循环必须拒绝。
- 基础因果图和点击后追加的投影图在返回调用方前都必须完成闭包断言并深度冻结。
- 主要旧页面的可见领域结果必须能够直接进入唯一因果节点，并保存返回原页面继续浏览的上下文；定位失败不得静默忽略。
- 解释、名称与观察文案只能由事实和因果数据派生，不得反向写回模拟事实。
- 纯界面浏览不进入宇宙历史；未来若宇宙规则允许观察产生作用，该观察必须成为显式因果输入。
- 二维观察的摘要、坐标／尺寸／亮度、各强度值和事件关联必须各自引用实际计算操作数；强度值与来源主题由同一份投影度量定义生成，区域外文明或神话不得成为局部强度原因。Seed 对比必须建立绑定左右生成身份、左右来源节点、两侧评分、差值与固定顺序裁决的组合证据；左右随机历史仍保留在各自因果图中，不得合并或伪造跨图边。
- 步骤 2 的 `UniverseDefinition`、`UniverseState`、逻辑时钟、随机流、转换、差异、事件、运行因果网络和存档均使用独立契约版本，不能借用旧规则或步骤 1 因果版本冒充兼容身份。
- 运行事件只能从已提交差异重新投影；历史对象查看只允许反向应用差异副本，不能修改当前状态、输入日志、随机流或模拟位置。
- 状态转换与存档任务必须完整提交或保持原值；保存、恢复和连续运行不得并发提交同一时间步。
- 观察信号必须绑定宇宙定义、被观察逻辑时刻对应的唯一状态、对象、观察方式和来源引用；跨宇宙信号或同历史错误状态身份不得进入同一研究记录。
- 观察信号的内容哈希不得充当来源真实性证明；加载研究记录时必须使用当前可信运行历史重新核对结论和来源。
- 研究记录条目 ID 和顺序必须唯一，关注与观察历史只能引用已保存信号，笔记与推测只能引用已保存证据。
- 玩家研究数据不得自动升级为运行事实、规则、状态转换、随机输入或因果节点。
- 正式分支必须分别校验宇宙定义 ID、分支 ID、检查点 ID、状态哈希和历史哈希；当前状态收敛不得合并不同历史。
- 界外实验只能创建新分支，宇宙内干预只能作为正式转换输入进入当前历史；任何修改过去的操作不得覆盖父分支。
- 创世条件包只携带共同起点，历史分支包必须携带分支、状态、历史、随机流和完整性信息；接收者继续操作时创建自己的子分支。
- 研究记录必须绑定正式分支历史身份，只有与当前根分支可信重放完全匹配的步骤 3 记录可以迁移，其他跨分支记录明确拒绝。

## 5. 错误处理

- 旧生成入口继续校验 Seed、规则版本、模板和干预。
- 新因果校验错误必须使用稳定问题码并提供中文说明。
- 重复节点／边、悬空引用、非法根、输入清单错配、孤立节点、缺失或未知规则、邻接不一致、无根路径、非法循环和无效随机引用不得静默忽略。
- 因果查询目标不存在时应显示明确空状态，不得自动替换为另一个结果并隐瞒错误。
- 无效领域目标不得静默回退，必须抛出带错误码和字段路径的 `UniverseInputError`。
- 因果投影引用不存在、重复或类型不匹配的领域主题时必须明确拒绝，不能自动改指向其他对象。
- 损坏或不支持的分享载荷必须明确拒绝、受控迁移或静态只读打开。
- 存储写入失败时不得先提交内存成功状态；导入并发时不得覆盖导入开始后的本地变更。
- 运行存档公开拒绝码必须分别覆盖无效 JSON、无效形状、未知版本、校验和错误、身份错配、状态错配和历史错配；缺失输入、随机流或转换历史必须明确拒绝。
- 浏览器完整运行数据只能写入版本化 IndexedDB；数据库不支持、升级阻塞、事务失败和读取失败必须形成可访问错误，不得回退到 `localStorage` 保存完整状态。
- 研究存档的无效 JSON、未知版本、校验和错误、宇宙或历史身份错配、重复 ID、无效顺序和悬空信号或证据引用必须明确拒绝。
- 研究记录保存失败必须保留先前有效记录并显示可访问错误；页面不得先显示成功再异步回滚。

## 6. 发布与进程管理

- 发布前必须执行 `npm run check:release`。
- 本地手动启动开发服务使用 `npm run dev`，停止方式是在同一可见终端按 `Ctrl + C`。
- 本地手动启动预览服务使用 `npm run preview -- --port 4173`，测试结束后必须按 `Ctrl + C` 停止。
- 测试和发布检查结束后必须确认 4173、5173 无监听，且没有命令行指向本项目的 Node 进程残留。
- 任务结束前必须确认没有 Vite、Playwright 或其他测试服务器残留，并清理本次生成且无需交付的覆盖率、构建和测试报告目录。
