# 旧产品模块迁移矩阵

版本：1.0  
状态：产品重构实施路线的强制组成部分

本文件逐项规定当前旧产品模块在新产品路线中的最终处理方式。任何模块都必须明确归入保留、保留并增强、降级、替换或删除之一，不允许以“后续再看”作为阶段完成条件。

## 1. 处理方式定义

- 保留：现有职责符合新产品定义，只允许进行兼容性和工程质量调整。
- 保留并增强：保留核心职责，但必须补充新产品要求的数据、接口或行为。
- 降级：模块不再充当事实来源、核心流程或一级入口，只作为展示、兼容或条件视图存在。
- 替换：旧模块在负责步骤完成后退出新产品主流程，由新的状态、规则或交互模块承担职责。
- 删除：旧行为不进入新运行时；如需读取历史数据，只允许存在于隔离的只读兼容路径中。

每个步骤完成时，负责该步骤的矩阵条目必须具备实现、迁移、测试和文档四类证据。未完成的条目不得通过修改状态名称、保留隐藏入口或降低测试门槛规避验收。

## 2. 模拟核心模块

| 当前模块 | 当前职责 | 处理方式 | 负责步骤 | 完成定义 |
| --- | --- | --- | --- | --- |
| `src/sim/random.ts` | Seed、命名随机流与确定性抽样 | 保留并增强 | 步骤 1、2、7 | 记录 PRNG 算法版本、流身份、抽样序号与可序列化游标；存档恢复后的下一次抽样与不中断运行完全一致 |
| `src/sim/templates.ts` | 十种固定模板、六领域权重和风味偏置 | 替换 | 步骤 5 | 模板改为宇宙宪法预设；旧 `templateId` 只用于旧存档映射，不再限制本体、空间、指标和事件结构 |
| `src/sim/laws.ts` | 固定领域法则与固定交互关系 | 替换 | 步骤 1、5 | 步骤 1 先接入统一因果来源，步骤 5 再由可组合规则、约束、优先级和冲突裁决引擎取代固定领域实现 |
| `src/sim/metrics.ts` | 一次性生成固定全局指标 | 降级 | 步骤 1、2、3、5 | 指标只作为状态或观测的派生投影，不作为世界事实来源；指标集合由宇宙宪法和观察方式决定，不强制生命、文明等维度 |
| `src/sim/universe.ts` | 一次性生成完整宇宙并组装 `UniverseSummary` | 替换 | 步骤 1、2 | 步骤 1 将其降为旧基线兼容编排，步骤 2 由 `UniverseState`、模拟时钟和状态转换运行时取代一次性完整生成 |
| `UniverseSummary` 聚合契约 | 同时承载事实、展示、分享和存档恢复结果 | 降级后替换 | 步骤 1、2、7 | 步骤 1 仅作为兼容读取模型，步骤 2 不再作为事实来源，步骤 7 只允许在旧存档只读查看器中保留旧版本 |
| `src/sim/timeline.ts` | 预生成完整纪元与事件列表 | 替换 | 步骤 1、2、5 | 事件由真实状态差异生成；未来事件不提前物化；事件分类由宇宙宪法决定，不固定要求生命、文明、神话和终局纪元 |
| `src/sim/timeline-impact.ts` | 把预生成事件反算为空间、生命和文明生成偏置 | 删除 | 步骤 2 | 新运行时中状态产生事件，事件不得反向生成既有世界状态；旧字段只允许用于旧存档只读解释 |
| `src/sim/galaxies.ts` 的空间对象生成 | 一次性生成固定“星系—恒星系—行星”样本树 | 替换 | 步骤 2、5 | 空间对象成为持续状态；空间拓扑由宇宙宪法定义；不存在传统空间或传统天体的宇宙仍能完成演化、观察和存档 |
| `src/sim/galaxies.ts` 的生物圈与文明种子 | 达到阈值后即时生成生命、能力和文明倾向 | 替换 | 步骤 2、6 | 生命的出现、变化和消亡由状态转换产生；无生命宇宙不创建占位事件、文明种子或生命专属指标 |
| `src/sim/civilizations.ts` 的文明实体 | 文明诞生时预赋路径、终局和完整历史 | 替换 | 步骤 6 | 文明仅在规则允许且状态满足时出现，并通过感知、记忆、认知、意图和行动形成历史，不预赋未来与终局 |
| `src/sim/civilizations.ts` 的神话档案 | 把文明信仰、叙述和实际神性混为同一档案 | 替换 | 步骤 5、6 | 宇宙事实、实体信念、神话叙述和真实自主实体分离存储并分别追因 |
| `src/sim/interventions.ts` 与 `intervention-domain.ts` | 固定奇迹、目标、奇迹点、压力和反噬覆盖层 | 替换 | 步骤 4、5 | 由通用实验输入、宇宙内干预和宇宙宪法定义的可用作用取代；固定奇迹清单与强制反噬不进入新运行时 |
| `src/sim/contracts/interventions.ts` | 把玩家预设为观察者或奇迹施放者 | 替换 | 步骤 4 | 分离 `ExperimentInput` 与 `InUniverseIntervention`；二者共用可追踪协议，但权限、可观察性和历史语义不同 |
| `src/sim/explain.ts` 的解释 | 根据结果拼装原因与摘要文案 | 降级 | 步骤 1、3、7 | 解释只能渲染因果事实或明确视角，不得成为事实来源，也不得提前宣布尚未发生的终局 |
| `src/sim/explain.ts` 的 `observationLog` | 自动挑选重要事件、稀有发现与潜在终局 | 删除并替换 | 步骤 3 | 删除系统预判式日志，替换为由观测证据、内部观点、玩家笔记和玩家推测组成的研究记录簿 |
| `src/sim/names.ts` | 生成名称、标语和描述 | 降级 | 步骤 1、7 | 仅作为展示和创作投影，所有事实性表述必须回链到状态或因果节点，名称本身不得驱动世界状态 |
| `src/sim/compare.ts` | 比较两个独立 Seed 的静态法则差异 | 替换 | 步骤 4、5 | 主要比较能力改为共同祖先分支的状态与因果差异；跨宇宙比较保留为独立工具并明确没有共同历史 |
| `src/sim/share.ts` | 单一分享码同时编码创世条件与旧干预数组 | 替换 | 步骤 4、7 | 分为创世条件包、可继续历史分支包和静态创作档案；不同玩家继续操作时创建新分支 |
| `src/sim/validation.ts` 与 `errors.ts` | 校验旧生成、模板、奇迹与分享输入 | 保留并增强 | 所有步骤 | 改为校验宇宙宪法、状态转换、分支、实验、干预、存档和分享类型；错误必须保留字段路径与稳定错误码 |
| `src/sim/contracts/*` | 保存基础事件、指标和干预契约 | 替换 | 步骤 1、2、3、4、5、6、7 | 契约按事实、状态、因果、观察、分支、规则包、自主实体和长期存档重新分层，旧固定纪元与固定奇迹类型退出新运行时 |
| `src/sim/types.ts` 与 `src/sim/index.ts` | 聚合导出旧 `UniverseSummary`、固定领域类型和生成 API | 替换 | 步骤 1、2、3、4、5、6、7 | 公共 API 改为导出宇宙定义、状态、因果、观察、分支、规则包、自主实体和存档契约；旧类型只从隔离兼容入口导出 |
| `src/sim/content/*` | 提供固定法则、事件、空间、文明和奇迹素材池 | 降级 | 步骤 2、5、6、7 | 内容只能描述规则允许的表现形式，不能决定事实是否发生；预制事件池不得继续生成历史，固定文明路径、神话类型与文明历史蓝图由步骤 6 退出事实生成 |
| `src/sim/recipes/*` | 保存旧阈值、权重和路径决策 | 替换 | 步骤 2、5、6 | 可复用公式进入规则包或状态转换系统，绑定旧模板、固定生命路径和文明终局的公式删除 |

## 3. 界面与交互模块

| 当前模块 | 当前职责 | 处理方式 | 负责步骤 | 完成定义 |
| --- | --- | --- | --- | --- |
| `src/main.tsx` | 挂载单一网页应用 | 保留并增强 | 所有步骤 | 继续作为应用入口，只装配新的应用根与错误边界，不直接持有模拟、观察或存档状态 |
| `src/App.tsx` 与 `useUniverseAppModel.ts` | 围绕一次性 `UniverseSummary` 装配九个功能页面 | 替换 | 步骤 2、3、4 | 改为围绕运行中宇宙、观察上下文、实验分支和持久状态装配；不得继续在任意输入变化后重新生成完整宇宙 |
| `AppChrome.tsx` 的 `UniverseToolbar` | Seed、固定模板、创世、随机和单一复制分享 | 替换 | 步骤 3、4、5 | 拆为创世条件、运行控制、实验与分享入口；不同分享类型必须明确标注，不再把模板与单一分享码作为永久主交互 |
| `src/ui/clientSeed.ts` | 为旧创世工具栏生成客户端 Seed | 保留并增强 | 步骤 4、5 | 继续提供显式创世条件标识，但不能读取隐式时间作为模拟事实；生成方式、来源和是否进入因果根必须明确记录 |
| `AppChrome.tsx` 的 `PageNavigation` | 永久展示概览、星系、文明、奇迹、纪元等九个一级栏目 | 替换 | 步骤 3 | 主导航围绕当前宇宙、观察、实验、历史分支和存档组织；对象类型作为上下文视图按宇宙实际能力出现 |
| `OverviewPage` | 平铺指标、事件、星系、文明、神话数量和主要入口 | 替换 | 步骤 2、3、5 | 步骤 2 接入运行状态与近期变化，步骤 3 改为当前可知状态、未知区域和玩家关注内容，步骤 5 验证页面不再硬编码传统空间、生命、文明或神话必然存在；未经观察的底层数量不得直接展示 |
| `TimelinePage` 与 `TimelinePanels` | 浏览预制纪元、完整未来和事件影响摘要 | 替换 | 步骤 1、2、3 | 步骤 1 接入真实因果查询，步骤 2 删除固定未来、旧时间滑块和 `timelineImpact` 展示，步骤 3 将已发生历史与观察、研究记录置于认知边界内呈现 |
| `LawsPage` | 平铺全部法则并静态比较另一个 Seed | 替换 | 步骤 1、3、4、5 | 区分玩家设定的公开公理、已观测规律、内部观点和未知规则；分支比较与跨宇宙比较分开呈现 |
| `LogsPage` | 展示系统生成的重要事件、稀有发现和潜在终局 | 删除并替换 | 步骤 3 | 删除旧日志页，替换为研究记录簿；系统不得替玩家预设重要性或提前展示终局 |
| `ObservationConsole` 与 `observationProjection.ts` | 将全部底层事实投影为固定二维节点与五类叠层 | 替换 | 步骤 2、3、5 | 消费观测信号而非底层全量事实；观察尺度、通道与视图由宇宙宪法和玩家工具决定 |
| `SpaceExplorer` | 固定浏览星系、恒星系、行星、生物圈和文明候选 | 降级 | 步骤 3、5 | 保留为传统空间宇宙的条件视图；通用探索入口不得依赖传统空间层级，非空间宇宙不显示该入口 |
| `CivilizationPanel` | 永久一级文明、神话和预制历史页面 | 降级并替换 | 步骤 3、6 | 改为条件出现的自主实体视图；没有文明的宇宙不显示空白文明入口，神话与事实分层展示 |
| `MiraclePanel` 与 `miracleTargets.ts` | 固定造物主奇迹、目标、点数、压力与清除干预 | 删除并替换 | 步骤 4、5 | 删除固定奇迹面板和清空历史操作，替换为分离的界外实验与宇宙内干预界面；修改过去只能创建分支 |
| `useShareController.ts` | 发布单一分享文本与链接，并处理剪贴板回退 | 替换 | 步骤 4、7 | 根据创世条件包、历史分支包和静态创作档案提供不同发布入口，明确每种内容的身份与可运行性，并保留文件下载和手动复制回退 |
| `shareState.ts` | 从 URL 参数接收分享，恢复 Seed、模板和旧干预载荷，并对损坏数据警告回退 | 替换 | 步骤 4、7 | 按分享类型执行接收、版本校验、迁移、明确拒绝或静态只读打开；历史分支恢复必须校验状态、历史与随机流，损坏数据不得静默丢失或冒充等价恢复 |
| `UniverseLibrary` | 以分享码为 ID 管理静态宇宙条目 | 替换 | 步骤 2、4、7 | 改为分支与档案管理器，明确创世条件、运行分支、检查点和创作档案的不同身份与操作 |
| `useUniverseSelection.ts` | 管理固定空间层级和文明选择 | 保留并增强 | 步骤 3、5、6 | 改为通用对象与关系选择上下文，传统空间和文明选择作为可选适配器 |
| `selectors.ts` | 从完整 `UniverseSummary` 派生统计和页面数据 | 替换 | 步骤 2、3 | 改为从可知状态、观察结果和当前分支派生视图，不得绕过认知边界读取底层事实 |
| `labels.ts` 与 `components/icons.tsx` | 将内部枚举转换为用户可读标签并提供图标 | 保留并增强 | 所有步骤 | 支持动态规则包和对象类型，不允许因未知类型回退为泄露内部枚举的文本 |
| `AppErrorBoundary`、`components/common.tsx` 与 `styles*.css` | 错误恢复、可访问结构与视觉基础 | 保留并增强 | 所有步骤 | 继续满足错误恢复、键盘、响应式和无障碍要求，并适配新的运行、观察和分支交互 |

## 4. 存档、分享与版本模块

| 当前模块 | 当前职责 | 处理方式 | 负责步骤 | 完成定义 |
| --- | --- | --- | --- | --- |
| `src/ui/archive.ts` 的 A1 条目 | 保存分享码、Seed、模板和少量用户元数据 | 降级 | 步骤 2、7 | A1 只作为旧基线只读导入格式，不得冒充新宇宙运行分支；转换为新宇宙时必须创建新身份并声明不等价 |
| `archiveStorage.ts` 的 `localStorage` | 保存全部 A1 图书馆数据 | 降级并替换 | 步骤 2、7 | `localStorage` 仅保留轻量偏好和迁移标记；浏览器中的状态快照、输入日志和因果数据使用版本化 IndexedDB 存储适配器 |
| `useUniverseArchive.ts` | 读写、收藏、搜索、导入和恢复 A1 | 替换 | 步骤 2、4、7 | 改为管理宇宙定义、分支、检查点、观察记录和创作档案，并保持写入原子性与失败恢复 |
| A1 JSON 文本框导入导出 | 复制整库复现参数 JSON | 替换 | 步骤 7 | 提供可下载、可校验的创世条件包、历史分支包和静态创作档案；导入前明确类型、版本、可运行性和迁移结果 |
| current-only 规则运行时 | 只支持当前规则，旧分享按当前版本警告回退 | 替换 | 步骤 4、7 | 未知版本只能受控迁移、明确拒绝或作为静态快照打开，禁止使用不同规则生成并冒充原宇宙 |
| 单一 `shareCode` 身份 | 同时充当宇宙、干预分支和存档 ID | 删除并替换 | 步骤 4 | 分离宇宙定义 ID、分支 ID、检查点 ID、状态哈希和历史哈希；状态收敛不得合并不同历史 |

## 5. 测试、文档与工程门禁

| 当前模块 | 当前职责 | 处理方式 | 负责步骤 | 完成定义 |
| --- | --- | --- | --- | --- |
| `tests/phase-1.test.ts` 至 `phase-8.test.ts` | 同时验证永久不变量与旧产品内容形状 | 降级并拆分 | 步骤 1 至 7 | 永久不变量迁入新契约测试；固定事件、纪元、对象、文明和奇迹数量只保留为旧基线兼容测试，并在对应替换步骤退出主门禁 |
| `tests/helpers.ts` 的完整宇宙断言 | 强制预制时间线和旧聚合形状 | 替换 | 步骤 1、2 | 改为验证因果闭包、状态合法性、确定性转换和可继续恢复，不再强制旧对象数量 |
| UI 与 E2E 测试 | 验证九页导航、奇迹操作、分享恢复和 A1 图书馆 | 替换 | 步骤 1 至 7 | 步骤 1 先由新因果 UI 与 E2E 接管“可见结果必须可追因”的职责；步骤 2 至 7 每步增加可运行纵向闭环测试，旧页面测试随对应模块替换同步迁移或删除 |
| `tests/architecture.test.ts` | 保护当前静态生成与 UI 分层 | 保留并增强 | 所有步骤 | 每一步先定义新边界再更新门禁，禁止旧架构规则阻止状态运行时、观察层和分支系统进入主流程 |
| `docs/architecture.md` | 描述当前一次性生成流水线 | 替换更新 | 每个步骤 | 当前内容仅作为基线；每一步完成前必须改写为实际新架构，静态流水线不得继续作为未来扩展要求 |
| `docs/quality-gates.md` | 把旧阶段测试和奇迹交互作为长期门禁 | 替换更新 | 步骤 1 | 区分永久不变量、旧基线兼容和新步骤验收，旧产品形状不得成为永久发布门禁 |
| `docs/non-functional-requirements.md` | 固定旧生成速度、Seed、模板、奇迹和 I1 分享要求 | 替换更新 | 步骤 1、2、4、7 | 保留通用性能、可靠性和无障碍目标，按运行时、观察、分支和长期存档重新定义预算与兼容策略 |
| `CONTRIBUTING.md` | 同时包含长期工程原则和 current-only、`timelineImpact`、文明种子、固定奇迹、I1、A1 等旧基线开发约束 | 保留并持续更新 | 每个步骤 | 长期原则继续有效；旧基线约束必须显式标注兼容范围和退出步骤，并在步骤 2 至步骤 7 接管对应职责时同步删除或改写，不得继续误导新实现 |
| `README.md` 与 `docs/README.md` | 提供项目入口、当前实施状态和事实来源导航 | 保留并持续更新 | 每个步骤 | 每一步完成前必须与里程碑、质量门禁和对应步骤规格保持一致，不得发布陈旧测试数字、阶段状态或旧产品事实来源 |
| 归档阶段文档 | 保存旧产品规格和完成证据 | 保留 | 所有步骤 | 只读归档，不得重新作为新产品验收来源；有效约束必须迁入当前文档 |

## 6. 分步骤关闭范围

- 步骤 1 关闭因果契约、解释来源、旧测试分层、当前架构与门禁换轨，并把 `UniverseSummary` 降为兼容读取模型。
- 步骤 2 关闭一次性生成器、预制时间线、`timelineImpact`、固定状态来源、不可继续存档和旧时间浏览问题。
- 步骤 3 关闭旧 Overview、Logs、九页后台式导航、全知观察和固定一级对象入口问题。
- 步骤 4 关闭单一分享码、无分支身份、可清空历史、实验与干预混同以及旧 Seed 对比问题。
- 步骤 5 关闭固定模板、固定法则领域、固定指标、固定空间拓扑、固定观察叠层和固定奇迹清单问题。
- 步骤 6 关闭即时生命种子、预制文明历史、预赋终局以及神话叙述与真实实体混同问题。
- 步骤 7 关闭 A1 作为运行存档、`localStorage` 大型状态存储、current-only 冒充恢复和单一 JSON 文本框导出问题。

步骤 7 完成后，本矩阵不得存在“仅隐含覆盖”“待决定”或没有完成证据的条目。仍需保留的旧能力必须位于明确隔离的只读兼容路径中。

## 7. 步骤 1 责任与证据台账

状态含义如下：

- “步骤 1 范围已实现”只表示该条目的步骤 1 子职责已有实现和测试来源，不表示跨步骤条目已经最终关闭。
- “登记完成”表示旧形状已明确分类，但仍由后续步骤执行删除或替换。
- “完整门禁已通过”表示实现已经通过当前质量门禁规定的静态检查、自动化测试、构建、体积和真实浏览器验收，但仍不等于严格评审完成。
- “待全新严格评审”表示实现与完整门禁已经关闭，尚需由未参与实现的全新评审者检查 P0、P1、P2 问题；有效 P0、P1 关闭且 P2 均明确处置后才能宣布步骤完成。

| 编号 | 对应矩阵条目 | 步骤 1 状态 | 实现证据 | 测试证据 | 文档证据 | 兼容证据与后续关闭 |
| --- | --- | --- | --- | --- | --- | --- |
| S1-01 | `src/sim/random.ts` | 第四轮 P1/P2 整改已关闭 | 随机决定保存完整 Seed 材料、原始样本、流、作用域、类型化操作参数、候选、权重和选中值；受控工厂重放完整生成调用 | 六类操作互换、参数修改、候选语义、作用域错配、多场景反向覆盖负向用例 | `architecture.md` 第 3、5 节、第四轮整改报告 | 旧随机序列保持；可序列化游标与恢复后下一抽样由步骤 2、7 关闭 |
| S1-02 | `src/sim/laws.ts` | 步骤 1 范围已实现 | 法则领域、法则与法则关系进入统一因果图 | 可见结果覆盖、规则引用和根可达用例 | `step-1.md`、`architecture.md` | 固定六领域继续兼容；可组合规则包由步骤 5 替换 |
| S1-03 | `src/sim/metrics.ts` | 步骤 1 范围已实现 | 七个旧指标作为带原因和规则的派生节点 | 指标目标覆盖与闭包用例 | `step-1.md`、退役登记 H-02/H-08 | 固定指标仍供旧页面读取；状态和观察派生由步骤 2、3、5 关闭 |
| S1-04 | `src/sim/universe.ts` | 步骤 1 范围已实现 | `generateUniverse` 保留旧兼容懒物化路径，`generateCausalUniverse` 作为产品主流程强制物化、重放比对并校验因果图 | 相同完整输入的领域结果、随机追踪与因果图确定性用例 | `architecture.md` 第 2 节 | 旧创世、浏览、分享和 A1 路径保留；一次性生成器由步骤 2 替换 |
| S1-05 | `UniverseSummary` | 步骤 1 范围已实现 | 新增不可枚举、按需物化、缓存且运行时冻结的只读 `causalGraph`，普通领域 JSON 不携带巨型因果图，旧字段继续作为兼容读取模型 | 原阶段回归测试、步骤 1 契约测试与运行时冻结用例 | `step-1.md` 第 6、9 节 | 不允许新增领域事实只存在于聚合对象；步骤 2、7 继续降级和隔离 |
| S1-06 | `src/sim/timeline.ts` | 第二轮 P1 整改已关闭 | 旧事件、触发关系、效果和来源被映射为因果节点与边，事件数量决定进入集合边界节点并成为每个成员的直接原因 | 时间线结果覆盖、集合数量决定反向覆盖、双向邻接和根可达用例 | `architecture.md` 第 4、5 节、第二轮整改报告 | 预制未来与固定纪元仍属旧兼容，步骤 2、5 替换 |
| S1-07 | `src/sim/explain.ts` 的解释 | 步骤 1 范围已实现 | 解释只生成 `explanation` 派生节点 | “解释不是事实根”用例 | `step-1.md`、`architecture.md` | 旧文案保留显示；认知边界与创作输出由步骤 3、7 关闭 |
| S1-08 | `src/sim/names.ts` | 步骤 1 范围已实现 | 名称、标语和描述只通过宇宙身份／展示投影追因 | 可见结果覆盖与根可达用例 | `architecture.md` 第 4 节 | 名称不驱动事实；正式创作投影由步骤 7 关闭 |
| S1-09 | `validation.ts` 与 `errors.ts` | 步骤 1 范围已实现 | 旧生成输入校验保留；因果校验使用独立稳定问题码，并覆盖重复端点、根列表、随机追踪、闭环授权和投影来源等问题 | 原输入负向测试与全部因果问题码直接负向测试 | `quality-gates.md` 第 3 节 | 步骤 1 问题码覆盖已关闭；后续输入类型随各步骤继续增强 |
| S1-10 | `src/sim/contracts/*` | 步骤 1 已关闭 | 因果契约为 `ugs-causality@13` 与 `ugs-random-bindings@5`；生产事实、受控公式投影、实体定位器、候选资格、全页面可见值与统计深层语义边界分离 | 生产绑定可解析且不可变，字段公式、候选顺序／权重／资格、精确随机决定、显示文本、统计分组与筛选证据通过定向验证 | `architecture.md` 第 3 节、首次评审报告、整改报告与两轮有限复查报告 | 步骤 1 进程内事实认证边界继续由完整门禁保护；跨进程存档认证和迁移由步骤 7 接管 |
| S1-11 | `src/sim/types.ts` 与 `index.ts` | 步骤 1 范围已实现 | 公共导出因果类型、查询、校验、契约版本和随机版本 | TypeScript 检查与步骤 1 契约测试 | `architecture.md` 第 1、3 节 | 旧 `UniverseSummary` 类型仍从主入口导出，步骤 2、4、5 继续隔离 |
| S1-12 | `src/main.tsx` | 步骤 1 范围已实现 | 保持单一应用挂载并按顺序加载两份因果样式 | 应用 UI 与真实浏览器因果入口用例 | `architecture.md` 第 8 节 | 未持有模拟状态；所有后续步骤继续复用入口 |
| S1-13 | `TimelinePage` 与主要旧结果页面 | 第十一轮 P1 整改已实现 | 实际渲染字段、集合数量、事件元数据、指标影响与条件分支使用统一结果契约；状态值投影记录公式、操作数与精确随机决定 | UI 结果描述符、架构裸值拒绝夹具与四类真实浏览器值级进入／返回用例 | `step-1.md` 第 9 节、第十一轮整改报告 | 旧页面仍为兼容路径；步骤 2 至 6 按矩阵继续替换其旧产品形状 |
| S1-14 | `LawsPage` | 首轮 P1 整改已关闭 | Seed 对比通过一等组合证据校验左右 Seed、生成身份、来源节点、评分、差值与最大差异裁决；单侧投影只陈述本侧评分与依据，左右图和随机历史保持独立 | 领域差值、最大差异、并列裁决、身份错配、缺失来源、键盘与真实浏览器追溯用例 | `step-1.md`、`architecture.md`、首轮整改报告 | 玩家认知、共同祖先分支比较和动态规则包由步骤 3、4、5 关闭 |
| S1-15 | `labels.ts` 与 `icons.tsx` | 步骤 1 范围已实现 | 因果界面使用中文领域标签和现有图标体系 | 组件文字、键盘和 axe 用例 | `non-functional-requirements.md` 第 3 节 | 动态未知类型的标签策略仍由步骤 5 扩展 |
| S1-16 | 错误边界、公共组件与样式 | 完整门禁已通过 | 因果组件拆分独立基础与响应式样式，继续使用顶层错误边界，新增文件均已纳入规模保护线 | UI axe、应用入口、构建、体积和真实浏览器用例 | `architecture.md` 第 7、8 节 | 步骤 1 的错误恢复、窄屏、键盘、无障碍和文件规模门禁已关闭 |
| S1-17 | 原 `phase-1` 至 `phase-8` 测试 | 登记完成 | 不修改旧业务实现，新增独立步骤 1 契约测试 | `tests/step-1-causality.test.ts` | `legacy-test-retirement.md` 第 3 节 | 固定形状继续保护步骤 1 兼容版本，按步骤 2 至 7 逐项退出 |
| S1-18 | `tests/helpers.ts` 完整宇宙断言 | 登记完成，未最终替换 | 旧聚合断言保持兼容；因果闭包使用独立测试帮助逻辑 | 步骤 1 因果覆盖、根可达和闭包用例 | 退役登记 H-01 至 H-15 | 固定数量不得解释为永久要求；步骤 2 起逐项移除 |
| S1-19 | `tests/architecture.test.ts` | 第十二轮裁定整改与完整门禁已关闭 | 架构门禁覆盖全部既有导出路径、结果渲染契约、真实值指纹与当前文档快照 | 21 项步骤 1 契约、2 项生产工厂守卫与 9 项架构测试，共 32 项定向测试 | `quality-gates.md` 第 5 节、第十二轮整改报告 | 步骤 1 认证边界、导入循环、文档一致性和规模保护线继续由当前门禁保护 |

当前定向测试组成：步骤 1 契约测试 21 项；生产工厂守卫 2 项；架构测试 9 项；合计 32 项。

当前发布快照：模拟测试 104 项；模拟覆盖率 92.26%／83.44%／94.52%／95.72%；UI 测试 44 项；UI 覆盖率 89.96%／81.96%／92.3%／92.76%；独立性能测试 1 项；E2E 54 项通过、2 项跳过；JavaScript gzip 87406 字节；CSS gzip 5828 字节。
| S1-20 | `docs/architecture.md` | 步骤 1 范围已更新 | 当前静态兼容流、因果层、查询边界和后续禁区均已落盘 | 文档一致性检查 | 本台账与架构文档 | 不再把静态 `UniverseSummary` 当作目标架构 |
| S1-21 | `docs/quality-gates.md` | 步骤 1 已关闭 | 永久不变量、步骤契约、旧兼容和退役登记已分层；新增候选资格、真实值指纹、显示文本、统计深层语义、当前文档全文一致性和独立性能门禁 | 104 项模拟、44 项 UI、1 项独立性能、54 项 E2E 通过，2 项 E2E 跳过，覆盖率、构建和体积通过 | 质量门禁第 2、3、4、5、7 节 | 第二轮有限复查确认 P0、P1、P2 和建议项均为零 |
| S1-22 | `docs/non-functional-requirements.md` | 第十二轮范围与完整门禁已关闭 | 增加候选资格、真实值指纹、统计深层证据、当前事实唯一性和 P50／P90 性能采样 | 21 项步骤 1 契约测试、2 项生产工厂守卫、9 项架构测试及 1 项独立性能测试 | 非功能需求全文、体积预算与 UI 运行时决策 | 状态、观察、分支和长期存档数值预算由后续步骤补齐 |
| S1-23 | 归档阶段文档 | 步骤 1 范围已满足 | 归档保持只读，未作为新实现规格修改 | 文档目录核对 | `docs/archive/README.md` | 有效约束已迁入当前文档，归档不作为完成证据 |
| S1-24 | 步骤 1 新因果界面 | 步骤 1 范围已实现 | `CausalExplorer.tsx`、`causalExplorerModel.ts`、随机候选证据、App 导航与装配 | 独立组件、真实应用、键盘、axe 和 E2E 因果用例 | `step-1.md` 第 5、9 节 | 这是步骤 1 新能力，不改变旧页面在步骤 2 至 7 的退出责任 |
| S1-25 | 观察台与 Seed 法则对比追因 | 首轮 P1 整改已关闭 | 每项观察强度由同一 `ObservationMeasure` 同时产生数值与来源主题，文明、魔法和神性只登记当前区域实际操作数；法则比较使用左右组合证据并在追加投影后重新校验闭包 | 五类强度逐项重算、区域外文明排除、左右组合证据、零结果候选、键盘、axe 与 E2E 用例 | `architecture.md`、`quality-gates.md`、首轮整改报告 | 当前仍消费旧观察与独立 Seed 对比形状；步骤 2、3、4、5 分别接管真实历史、认知边界、分支比较和动态观察通道 |
| S1-26 | `README.md` 与 `docs/README.md` | 步骤 1 范围已更新 | 项目入口、文档阅读顺序、当前状态与发布门禁证据保持一致 | 文档检索、链接校验与发布门禁结果 | 本台账、里程碑与质量门禁 | 后续每个步骤完成前同步更新，不得重新出现阶段状态漂移 |

步骤 1 已完成。冻结后首次完整评审的两项 P1 已经两轮有限复查关闭，最终有效 P0、P1、P2 和建议项均为零。步骤 2 只能在自己的冻结验收基线落盘后开始实现。
