# 步骤 1 第六轮严格复审整改报告

整改日期：2026-07-14

对应评审：[步骤 1 第六轮严格架构与产品复审报告](step-1-review-06.md)

## 1. 整改结论

第六轮复审登记的一项 P1 与两项 P2 已完成整改，完整发布门禁已经通过。步骤 1 仍不能标记完成，必须由第七名全新 agent 复审归零。

## 2. P1-01：类型化领域定位与真实结果核对

- 因果契约升级为 `ugs-causality@7`，随机结果绑定升级为 `ugs-random-bindings@3`。
- 删除启发式 `outputPath`，改用根字段、映射键、实体 ID、集合数量和负事实谓词五类可判别联合定位器。
- 构建器在节点规格进入时根据受控重放领域对象独立记录定位器和值指纹，最终公开绑定从稳定节点另行生成。
- 生产认证重新解析全部定位器，确认映射键、实体、所属集合和负事实谓词真实存在，并核对规范化领域值指纹。
- 未知节点类型、错误主题格式、缺失实体、负事实不成立和值指纹变异均明确拒绝。
- 领域索引按一次受控重放建立，避免九千余结果节点产生逐项全表扫描。

## 3. P2-01：逐字段随机转录比较和完整变异矩阵

- 新增不写入认证状态的 `randomTraceMatchesExpected`，逐字段比较追踪、流、决定、操作、类型化参数、作用域、候选和结果。
- 直接覆盖六类操作整体互换，以及 `range` 和 `int` 双边界、`bool` 概率、`pick` 顺序与内容、`weighted` 顺序、标签和权重变异。
- 新增生产工厂守卫测试，通过完整 `buildUniverseCausalGraph` 入口分别命中随机调用转录不一致和随机结果绑定转录不一致分支。

## 4. P2-02：AST 认证调用图门禁

- 架构门禁使用 TypeScript AST 收集模块内函数声明、函数表达式和箭头函数。
- 门禁从全部导出函数执行调用图可达性分析，当前只有 `buildUniverseCausalGraph` 可以到达私有 `certifyBuiltGraph`。
- 新增“导出入口 → 私有 helper → 认证写入”的间接夹具，证明绕行路径会被识别。
- 拆分领域定位、绑定结构校验、转录比较和定位器契约，消除新增循环依赖并保持文件规模门禁。

## 5. 定向验证

- TypeScript 类型检查通过。
- 步骤 1 契约测试 18 项、生产工厂守卫 2 项、架构测试 7 项，共 27 项定向测试通过。
- 代表性旧宇宙首次物化、双向查询和缓存复用仍低于普通环境 1500 毫秒预算。
- 模拟核心内容哈希更新为 `073f116cfec81e6e4dc11b52058ce9f6abe0d15dcb66dce219a8c2795042c34c`。

## 6. 完整发布门禁

- 模拟测试 99 项通过，覆盖率为 93.91%／86.81%／95.22%／95.73%。
- UI 测试 39 项通过，覆盖率为 89.14%／81.74%／91.07%／91.97%。
- E2E 42 项通过、2 项按平台能力跳过。
- JavaScript gzip 为 112633 字节，CSS gzip 为 5721 字节，均通过保护线。
- ESLint、TypeScript、生产构建和完整 `npm run check:release` 全部通过。

仍必须由未参与实现、前六轮评审或整改的第七名全新 agent 复审，P0、P1、P2 全部为零后才能关闭步骤 1。
