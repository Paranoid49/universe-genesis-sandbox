# 步骤 2 首次独立评审第一轮有限复查报告

复查范围：首次评审的 7 项 P1、2 项 P2、2 项建议项，本轮整改及其直接回归。

复查结论：P1-01 至 P1-05、P2-01 和迁移接口建议已经关闭；P1-06、P1-07、P2-02 仍未关闭，步骤 2 暂不可完成。

## 1. 未关闭问题

### P1-06：暂停性能门禁采样与真实路径不足

原测试只有四个单次调度器样本，没有执行两次预热和至少十二个多模板样本，也没有经过运行模型接收暂停命令、提交暂停状态和清理生产调度器的完整路径，不能证明冻结第 6.3 节的稳定预算。

### P1-07：真实键盘 E2E 使用测试代码直接聚焦

原 E2E 使用 Playwright `.focus()` 跳转到每个控件，没有证明玩家可以通过 Tab、Shift+Tab、Enter、Space 和方向键实际到达控件，也没有验证从运行因果查询返回原上下文。

### P2-02：当前文档状态仍然矛盾

`legacy-test-retirement.md`、根 README 和文档导航仍保留等待首次评审或等待首次整改门禁的过期描述，与首次评审、整改报告和已经完成的发布门禁证据不一致。

## 2. 已关闭问题

- P1-01 运行控制污染世界身份已关闭。
- P1-02 状态差异不完整已关闭。
- P1-03 存档缺少转换语义校验已关闭。
- P1-04 存档失败后继续推进已关闭。
- P1-05 任意查询参数进入旧产品已关闭。
- P2-01 IndexedDB 原子失败证据不足已关闭。
- 相同参数重新创世建议不属于冻结要求，不阻塞。
- 显式存储迁移接口建议已关闭。

## 3. 第一轮复查验证

- 定向模拟测试 22 项通过。
- 定向 UI 测试 57 项通过。
- 独立性能测试 3 项通过。
- 四浏览器两条运行 E2E 共 8 项通过。
- Lint 与 TypeScript 类型检查通过。
- 未发现新的状态、存档、兼容或因果直接回归。

步骤 2 必须完成三项剩余问题的第二轮整改，并由另一名全新独立 Agent 执行第二轮有限复查。
