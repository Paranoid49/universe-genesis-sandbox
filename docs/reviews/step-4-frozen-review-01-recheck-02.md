# 步骤 4 首次独立评审第二轮有限复查报告喵

## 一、复查范围与总体结论喵

本次复查严格以 `docs/step-4-freeze.md` 为唯一冻结验收基线，只核验第一轮有限复查仍未关闭的 P1-07、由 P1-03 降级而来的 P2 共同历史范围真实性问题、第一轮复查后的第二轮修改及其直接回归喵。

本次没有重新开放 P1-01、P1-02、P1-04、P1-05、P1-06 或 P2-01，没有评价步骤 5 及以后能力，也没有增加冻结要求或提高既有关闭标准喵。

复查结论为 P1-07 关闭、共同历史范围 P2 关闭，限定范围内没有发现由第二轮修改引入的有效 P0、P1 或 P2 直接回归喵。

| 复查项目 | 结论 | 当前严重等级 | 主要证据 |
| --- | --- | --- | --- |
| P1-07 | 关闭喵 | 无喵 | 360 像素纯键盘真实浏览器用例已经覆盖创建、切换、比较、保存、刷新恢复、两类分享、无横向溢出和 axe，并在四个浏览器项目通过喵 |
| P1-03 降级 P2 | 关闭喵 | 无喵 | `commonTransitionCount` 必须等于当前谱系末节点 `forkTick`，合法数值范围内的重签篡改已经被负向测试拒绝喵 |

## 二、P1-07 复查结论喵

### 结论为关闭喵

- 涉及位置为 `tests/e2e/app.spec.ts:170` 至 `tests/e2e/app.spec.ts:233` 喵。
- 现实触发方式为把视口设为 360×800，只使用 Tab、Enter、Space、组合键和键盘文本输入完成步骤 4 分支主流程喵。
- 当前用例先以键盘创建实验分支，再进入历史分支页切换回另一分支、执行共同祖先比较、保存当前分支、记录身份并刷新页面，刷新后重新通过键盘进入历史分支页并核对活动分支身份恢复喵。
- 当前用例随后继续通过键盘导入历史分支包、推进共享只读节点形成接收者子分支、导入创世条件包，并保留 360 像素无横向溢出和 axe 零违规检查喵。
- 咪亲自执行该用例，Chromium、Firefox、WebKit 和移动 Chromium 共 4 项全部通过，证明原先缺失的切换、比较、保存和刷新恢复证据已经补齐喵。
- 实际影响为冻结 C16 明列的 360 像素窄屏核心闭环现在具有真实浏览器与纯键盘证据，原先可能让焦点顺序、窄屏可操作性或活动分支恢复回归进入发布的证据缺口已经关闭喵。
- 冻结依据为 C16 对桌面与 360 像素窄屏完成分支创建、切换、比较、保存、恢复、分类型分享并通过键盘与 axe 检查的明确要求喵。

## 三、共同历史范围 P2 复查结论喵

### 结论为关闭喵

- 涉及位置为 `src/sim/branch-validation.ts:17` 至 `src/sim/branch-validation.ts:34` 和 `tests/step-4-validation.test.ts:47` 至 `tests/step-4-validation.test.ts:61` 喵。
- 现实触发方式为从已推进两步的根分支在时刻 1 创建子分支，把 `commonTransitionCount` 从 1 篡改为仍处于合法数值范围内的 0，并按公开确定性算法重新计算状态哈希、历史哈希和检查点身份喵。
- 当前校验先验证谱系完整性，再取当前谱系末节点的 `forkTick` 作为唯一允许的共同历史范围，并要求 `commonTransitionCount` 同时满足安全整数、非负、不超过当前转换数量和严格等于该分叉边界喵。
- 新增负向测试使用上述可重签篡改路径，`validateBranch` 明确抛出“共同历史范围无效”，咪亲自执行的步骤 4 核心与校验定向测试共 19 项全部通过喵。
- 历史分支包和分支存档解析都会进入同一个 `validateBranch` 校验路径，因此相同篡改不能通过保存、恢复或分享导入路径绕过喵。
- 实际影响为分支身份卡、存档与分享内容不能再声明与当前谱系分叉边界不一致的共同历史范围，第一轮复查登记的元数据真实性问题已经关闭喵。
- 冻结依据为 R1 对真实共同历史范围的要求、R6 对完整原因和输入顺序可验证的要求，以及历史元数据篡改威胁模型喵。

## 四、第二轮修改的直接回归核验喵

- 新增严格相等校验没有破坏普通根分支，因为普通根分支的谱系末节点 `forkTick` 与 `commonTransitionCount` 均为 0 喵。
- 新增严格相等校验没有破坏检查点根分支，因为检查点根分支以当前逻辑时刻同时设置谱系末节点 `forkTick` 和共同历史范围喵。
- 新增严格相等校验没有破坏普通子分支和共享节点首次继续，因为两者都以实际分叉时刻同时设置谱系末节点 `forkTick` 和共同历史范围喵。
- 分支存档与历史包仍复用统一校验器，未出现只在直接调用时拒绝、导入时绕过的分叉校验路径喵。
- 生产构建以退出码 0 完成，新增 E2E 在四个浏览器项目均通过，限定修改范围内没有发现具有现实触发路径并直接违反冻结基线的新回归喵。

## 五、亲自执行的验证证据喵

- `npx vitest run tests/step-4-validation.test.ts tests/step-4-branching.test.ts` 以退出码 0 完成，2 个测试文件共 19 项通过喵。
- `npm run build` 以退出码 0 完成，TypeScript 构建与 Vite 生产构建均通过喵。
- `npx playwright test tests/e2e/app.spec.ts --grep "360 像素下可用纯键盘完成分支与分类型分享核心闭环"` 以退出码 0 完成，Chromium、Firefox、WebKit 和移动 Chromium 共 4 项通过喵。
- 本次有限复查没有把整改报告中既有的完整发布门禁结果冒充为咪亲自执行的结果，仅采用上述定向验证支持本轮结论喵。

## 六、清理与进程确认喵

咪已删除本轮生成的 `coverage`、`dist`、`playwright-report` 和 `test-results`，并确认这些路径不存在喵。

咪已确认端口 4173 与 5173 均无监听，且没有命令行指向本项目工作区的 Node 进程残留喵。

用户手动启动开发服务器的命令如下喵。

```text
npm run dev
```

用户手动停止开发服务器的方式为在启动该命令的终端按 `Ctrl + C` 喵。

## 七、最终意见喵

P1-07 和由 P1-03 降级而来的 P2 均满足原关闭条件，可以关闭喵。

第二轮有限复查范围内不存在有效未关闭的 P0、P1 或 P2，步骤 4 首次独立评审的既有问题已经全部获得明确关闭处置喵。
